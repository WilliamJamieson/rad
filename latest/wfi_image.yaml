%YAML 1.1
---
$schema: asdf://stsci.edu/datamodels/roman/schemas/rad_schema-1.0.0
id: asdf://stsci.edu/datamodels/roman/schemas/wfi_image-1.4.0

title: Level 2 (L2) Calibrated Roman Wide Field Instrument (WFI) Rate Image.

datamodel_name: ImageModel
archive_meta: Science WFI Level 2

allOf:
  - $ref: asdf://stsci.edu/datamodels/roman/schemas/exposure/tweak_reg-1.0.0
  - type: object
    properties:
      meta:
        type: object
        properties:
          statistics:
            # Currently this is not being calculated anywhere and is being filled in by the background step.
            # in the L3 pipeline for now I am leaving here and required since it has L2 archive
            # metadata.
            $ref: asdf://stsci.edu/datamodels/roman/schemas/meta/statistics-1.0.0

        required: [statistics]
    required: [meta]
